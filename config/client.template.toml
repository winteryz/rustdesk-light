# Example rdl-client-gui/rdl-client-cli config.
# Priority:
# built-in defaults < this file < environment < startup arguments < embedded client config.

[server]
ip = "127.0.0.1"
port = 5169

[auth]
# token = "change-me"
